import 'package:flutter/material.dart';

class StudentQuizPage extends StatelessWidget {
  const StudentQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Student Quiz'),
      ),
    );
  }
}
